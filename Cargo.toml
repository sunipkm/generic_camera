[workspace]
resolver = "3"
members = [
  "gencam_asi_example",
  "generic_camera",
  "generic_camera_asi",
  "zwo_asi_sys"
]

[workspace.dependencies]
generic-camera = { path = "generic_camera", version = "0.0.12", default-features = false }
refimage = { version = "1.0.0-pre4", default-features = false }
